# Copy this file to .env and set VITE_API_URL to wherever the Flask backend is running.
# In development the default (http://localhost:5000) is used when this is unset.
VITE_API_URL=http://localhost:5000
